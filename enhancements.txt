Add feature:

As a user, I want to be able to ask the AI to execute sgpt commands.


When: User starts a conversation with "sgpt:"
Then: AI will respond back with a request to do the sgpt command.
If: The inquiry is for a shell command or code command
Then: AI will use sgpt --shell or --code options.
And: The sgpt command will be presented to the user with option to approve.
If: approved the ai will execute the command print the output to the user and give option to return to ai.

